name: wcase6 CI

on:
  push:
    branches: [ $default-branch ]
  pull-request:
    branches: [ $default-branch ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Run a one-line script
      run: echo Hellow from wcase6
